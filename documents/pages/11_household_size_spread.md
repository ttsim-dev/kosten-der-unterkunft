# The departure moves with household size

<img src="/kdu_vs_wohngeld/cap_ratio_spread_distribution.png" style="max-height: 345px; margin: 0.4rem auto;" />

Median spread within a Gemeinde **6.4 ratio points**, above 5 points in **55.9 %** of
them — so no single per-Gemeinde correction factor can work.

<!--
If the departure were a fixed per-Gemeinde offset, a lookup table of correction factors
would eliminate it. It is not: a single parent is size 2 in a Gemeinde whose difference you
would have calibrated at size 1, and the difference moves along the very dimension the
subgroup is defined on. n = 9,368 Gemeinden observed at every household size.
-->
