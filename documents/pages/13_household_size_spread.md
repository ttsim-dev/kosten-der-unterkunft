# One correction factor per Gemeinde is not enough

<img src="/kdu_vs_wohngeld/cap_ratio_by_household_size.png" style="max-height: 360px; margin: 0.2rem auto;" />

In **1 of 8 Gemeinden** the gap even changes direction: the local cap is above the proxy
for a single, below it for a family.

<!--
n = 9,368 Gemeinden observed at both sizes. Everything on the diagonal is a Gemeinde where
one correction factor would work; the width of the cloud around it is the argument, and
the two off-diagonal quadrants are the 12.1 per cent that flip. The aggregate hides this
completely: the median ratio is 0.960 at size one and 0.953 at size four, so a factor
calibrated nationally at size one looks fine on average and gets the direction wrong for
one Gemeinde in eight. Household size is the dimension the subgroups of interest — single
parents, couples with children — are defined on.
-->
